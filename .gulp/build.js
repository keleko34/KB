var gulp = require('gulp'),
    gulpClosureCompiler = require('gulp-closure-compiler'),
    gulpFile = require('gulp-file'),
    gulpInject = require('gulp-inject'),
    gulpReplace = require('gulp-replace');

module.exports = function()
{

  return function()
  {

  }
}
